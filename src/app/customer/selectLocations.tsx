import { View, Text } from 'react-native';
import React from 'react';

const SelectLocations = () => {
  return (
    <View>
      <Text>SelectLocations</Text>
    </View>
  );
};

export default SelectLocations;
